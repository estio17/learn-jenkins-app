pipeline {
    agent any

    stages {
        stage('w/o docker') {
            steps {
                sh ''' 
                    echo 'This stage runs without Docker.'
                '''
            }
        }
    }
}
